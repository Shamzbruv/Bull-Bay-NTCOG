# ── Node ──────────────────────────────────────────
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ── Runtime state (auto-generated, not for git) ──
runtime.json

# ── macOS ─────────────────────────────────────────
.DS_Store
.AppleDouble
.LSOverride

# ── Editors ───────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo

# ── Media (large binary assets — add manually) ───
# public/media/*.mp4
# public/audio/*.mp3
# (kept out of git; upload to Railway volume or CDN)
